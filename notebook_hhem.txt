["# 🛡️ Kaggle Hallucination Detection & Risk Assessment POC\n\nThis notebook evaluates various hallucination-detection approaches for a RAG-based support bot.\nIt is built to run on Kaggle's hosted environment with a GPU accelerator (T4 or P100).\n\n### Models Evaluated:\n1. **LettuceDetect** (`KRLabsOrg/lettucedect-base-modernbert-en-v1`)\n2. **Vectara HHEM-2.1-Open** (`vectara/hallucination_evaluation_model`)\n3. **MiniCheck-Flan-T5-Large** (Sentence-level, using correct 4-value unpacking signature)\n4. **Zero-Shot LLM Judge** (Local Llama 3.2)\n5. **Few-Shot LLM Judge** (Local Llama 3.2 with worked adjacent-capability examples)\n6. **Tiered/Composite Pipeline** (Relevance check + MiniCheck + LLM Judge Escalation)\n\n---", '---\n## ⚙️ Section 1: Model Initialization\nWe load LettuceDetect, HHEM, MiniCheck, and Relevance Embedders using explicit assertion checks to avoid silent loading failures.', '# ============================================================\n# 🧠 Load Models & Embedders\n# ============================================================\nimport traceback\nfrom sentence_transformers import SentenceTransformer\nfrom lettucedetect.models.inference import HallucinationDetector\nfrom transformers import AutoModelForSequenceClassification\nfrom minicheck.minicheck import MiniCheck\n\n# Initialize tracking dict for tool statuses\ntool_status = {\n    "Relevance": "SKIPPED",\n    "LettuceDetect": "SKIPPED",\n    "HHEM": "SKIPPED",\n    "MiniCheck": "SKIPPED"\n}\n\n# 1. Relevance Embedder\ntry:\n    print("Loading Relevance embedder (all-MiniLM-L6-v2)...")\n    relevance_embedder = SentenceTransformer(\'all-MiniLM-L6-v2\', device=DEVICE)\n    assert relevance_embedder is not None, "Relevance embedder failed to load"\n    print("  ✅ Relevance embedder loaded successfully!")\n    tool_status["Relevance"] = "LOADED"\nexcept Exception as e:\n    print("❌ Relevance embedder loading failed!")\n    traceback.print_exc()\n    relevance_embedder = None\n\n# 2. LettuceDetect\ntry:\n    print("Loading LettuceDetect (base-modernbert)...")\n    lettuce_detector = HallucinationDetector(\n        method="transformer",\n        model_path="KRLabsOrg/lettucedect-base-modernbert-en-v1"\n    )\n    assert lettuce_detector is not None, "LettuceDetect model failed to load"\n    print("  ✅ LettuceDetect loaded successfully!")\n    tool_status["LettuceDetect"] = "LOADED"\nexcept Exception as e:\n    print("❌ LettuceDetect loading failed!")\n    traceback.print_exc()\n    lettuce_detector = None\n\n# 3. HHEM\ntry:\n    print("Loading Vectara HHEM-2.1-Open...")\n    hhem_model = AutoModelForSequenceClassification.from_pretrained(\n        "vectara/hallucination_evaluation_model", trust_remote_code=True\n    ).to(DEVICE)\n    assert hhem_model is not None, "HHEM failed to load"\n    # Patch HHEM missing parameters weights\n    with torch.no_grad():\n        hhem_model.t5.transformer.encoder.embed_tokens.weight.data.copy_(\n            hhem_model.t5.transformer.shared.weight.data\n        )\n    print("  ✅ Vectara HHEM loaded successfully!")\n    tool_status["HHEM"] = "LOADED"\nexcept Exception as e:\n    print("❌ Vectara HHEM loading failed!")\n    traceback.print_exc()\n    hhem_model = None\n\n# 4. MiniCheck\ntry:\n    print("Loading MiniCheck (flan-t5-large)...")\n    minicheck_scorer = MiniCheck(model_name=\'flan-t5-large\')\n    assert minicheck_scorer is not None, "MiniCheck failed to load"\n    print("  ✅ MiniCheck loaded successfully!")\n    tool_status["MiniCheck"] = "LOADED"\nexcept Exception as e:\n    print("❌ MiniCheck loading failed!")\n    traceback.print_exc()\n    minicheck_scorer = None\n\nprint("\\nModel Load Status summary:", tool_status)\n']